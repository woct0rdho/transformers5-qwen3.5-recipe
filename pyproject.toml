[tool.ruff]
target-version = "py310"

[tool.ruff.lint]
extend-select = ["I"]

[tool.ty.analysis]
replace-imports-with-any = ["torch_ggml_ops", "triton", "triton.**"]

[tool.ty.environment]
python-version = "3.10"
